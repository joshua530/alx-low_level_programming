#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - gets string length
 *
 * @s: string to calculate tlength for
 *
 * Description: gets string length
 * Return: string length
 */
int _strlen_recursion(char *s)
{
	return strlen(s);
}
