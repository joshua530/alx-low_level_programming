#include <stdio.h>

/**
 * main - prints out a string
 *
 * Description: uses printf to print out a simple string
 *
 * Return: the exit value of the program
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
